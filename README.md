# JUnit Jupiter

JUnit Jupiter API 6.1.2 的测试生命周期、常用 Annotation 与断言适配，发布坐标为 `junit:jupiter:1`。

```powershell
norm test java-binding/junit-jupiter/junit/jupiter/GreetingTest.norm
```

测试类和方法均为普通 Norm 代码；JUnit Platform 负责发现、生命周期与结果汇总。
